class ZoneInfo {
  var zone;
  var name;
  var color;
  var fontColor;
  var perc;
  
  function initialize(zone, name, color, fontColor, perc) {
    self.zone = zone;
    self.name = name;
    self.color = color;
    self.fontColor = fontColor;
    self.perc = perc;
  }
}